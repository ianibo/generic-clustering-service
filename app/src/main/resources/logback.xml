<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>%cyan(%d{HH:mm:ss.SSS}) %gray([%thread]) %highlight(%-5level) %magenta(%logger{36}) - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="io.micronaut.data.query" level="WARN"/>
    <logger name="io.micronaut" level="INFO"/>
    <logger name="net.semweb" level="DEBUG"/>
    <logger name="gcs" level="DEBUG"/>

    <logger name="org.flywaydb.core.internal.dbsupport.SqlScript" level="WARN"/>
    <logger name="org.flywaydb" level="INFO"/>

    <!-- 
			<logger name="io.micronaut.data.query" level="DEBUG"/>
      <logger name="io.micronaut.flyway" level="DEBUG"/>
      <logger name="org.flywaydb" level="DEBUG"/> 
    -->

    <root level="warn">
        <appender-ref ref="STDOUT" />
    </root>

</configuration>
